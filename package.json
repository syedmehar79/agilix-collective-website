{
  "name": "agilix-collective-website",
  "private": true,
  "scripts": {
    "dev": "npx browser-sync start --config bs-config.js --open",
    "dev:serve": "npx browser-sync start --config bs-config.js",
    "deploy": "npx --yes vercel --prod --yes"
  },
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}
